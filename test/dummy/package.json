{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@katalyst-interactive/turbo-modal": "file:../..",
    "esbuild": "^0.13.6"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds"
  }
}
